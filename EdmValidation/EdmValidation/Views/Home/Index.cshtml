@model IEnumerable<EdmValidation.Models.tbl_Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.D_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HOD)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.D_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HOD)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.D_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.D_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.D_Id })
        </td>
    </tr>
}

</table>
